on: [push]

jobs:
  build_job:
    runs-on: ubuntu-latest
    name: A job to build
    steps:
    - name: Display directory context
      run: pwd
    - name: Display directory content
      run: ls
    - uses: ./
